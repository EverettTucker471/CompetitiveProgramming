#include <bits/stdc++.h>

using namespace std;

void triple_jump() {
    int n;
    cin >> n;

    int arr[n];
    for (auto& x : arr) cin >> x;

    int a = arr[0] / 3;
    int c = arr[n - 1] / 3;

    
}

int main() {
    triple_jump();
    return 0;
}